# Tag mixing periods - K=0.1

- Diagnostic reference: Derived at K=0.2
- Sensitivity value: Derived at K=0.1
- Input change: Use tag_flags column 1 containing the release-group mixing periods derived at K=0.1 in SC22-IP10

This folder is a complete, frozen model input set. The shared `mfclo64`
executable is copied here automatically when the model is run from the
repository root with `./run.sh <case-key>`.
