# Reporting rates applied during mixing

- Diagnostic reference: Reporting rates excluded during mixing
- Sensitivity value: Reporting rates applied during mixing
- Input change: Set tag_flags column 2 to zero so fitted reporting rates are applied within each release group's specified mixing period

This folder is a complete, frozen model input set. The shared `mfclo64`
executable is copied here automatically when the model is run from the
repository root with `./run.sh <case-key>`.
