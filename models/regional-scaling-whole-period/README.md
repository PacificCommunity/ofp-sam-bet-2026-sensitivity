# Regional scaling whole period

- Diagnostic reference: Current 5-year window
- Sensitivity value: periods 3-292
- Input change: Use all 290 mutually supported periods and parest flags 77-81 = 100 1 290 0 1

This folder is a complete, frozen model input set. The shared `mfclo64`
executable is copied here automatically when the model is run from the
repository root with `./run.sh <case-key>`.
